apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "soc4kafka.fullname" . }}
  labels:
    {{- include "soc4kafka.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  strategy:
    type: {{ .Values.strategy.type | default "RollingUpdate" }}
    {{- if eq (.Values.strategy.type | default "RollingUpdate") "RollingUpdate" }}
    rollingUpdate:
      maxSurge: {{ .Values.strategy.rollingUpdate.maxSurge | default "25%" }}
      maxUnavailable: {{ .Values.strategy.rollingUpdate.maxUnavailable | default "25%" }}
    {{- end }}
  selector:
    matchLabels:
      {{- include "soc4kafka.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "soc4kafka.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        checksum/config: {{ include "soc4kafka.configMapHash" . }}
        checksum/secrets: {{ include "soc4kafka.secretsHash" . }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.serviceAccount }}
      serviceAccountName: {{ include "soc4kafka.serviceAccountName" $ }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if .Values.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- end }}
      containers:
        - name: collector
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          args:
            - "--config=/etc/otel/config.yaml"
          env:
            {{- range .Values.splunkExporters }}
            {{- $secretName := .secret | default (printf "%s-hec-%s" (include "soc4kafka.fullname" $) .name) }}
            {{- if or .secret (and .token (not (hasPrefix "${" (toString .token)))) }}
            - name: SPLUNK_HEC_TOKEN_{{ .name | upper | replace "-" "_" }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: splunk-hec-token
            {{- end }}
            {{- end }}
            {{- range .Values.kafkaReceivers }}
            {{- $receiverName := printf "kafka/%s" .name }}
            {{- if .auth }}
              {{- if .auth.plain_text }}
                {{- if .auth.plain_text.password }}
                  {{- if kindIs "map" .auth.plain_text.password }}
                    {{- if .auth.plain_text.password.secret }}
                      {{- $envVarName := printf "KAFKA_%s_PLAIN_TEXT_PASSWORD" ($receiverName | upper | replace "/" "_" | replace "-" "_") }}
                      {{- $secretName := .auth.plain_text.password.secret }}
            - name: {{ $envVarName }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: password
                    {{- end }}
                  {{- end }}
                {{- end }}
              {{- end }}
              {{- if .auth.sasl }}
                {{- if .auth.sasl.password }}
                  {{- if kindIs "map" .auth.sasl.password }}
                    {{- if .auth.sasl.password.secret }}
                      {{- $envVarName := printf "KAFKA_%s_SASL_PASSWORD" ($receiverName | upper | replace "/" "_" | replace "-" "_") }}
                      {{- $secretName := .auth.sasl.password.secret }}
            - name: {{ $envVarName }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: password
                    {{- end }}
                  {{- end }}
                {{- end }}
              {{- end }}
              {{- if .auth.kerberos }}
                {{- if .auth.kerberos.password }}
                  {{- if kindIs "map" .auth.kerberos.password }}
                    {{- if .auth.kerberos.password.secret }}
                      {{- $envVarName := printf "KAFKA_%s_KERBEROS_PASSWORD" ($receiverName | upper | replace "/" "_" | replace "-" "_") }}
                      {{- $secretName := .auth.kerberos.password.secret }}
            - name: {{ $envVarName }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: password
                    {{- end }}
                  {{- end }}
                {{- end }}
              {{- end }}
            {{- end }}
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          ports:
            - name: health
              containerPort: 13133
              protocol: TCP
          {{- if .Values.startupProbe }}
          startupProbe:
            {{- toYaml .Values.startupProbe | nindent 12 }}
          {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /etc/otel
              readOnly: true
            {{- if .Values.persistence.enabled }}
            - name: data
              mountPath: /var/lib/otelcol
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "soc4kafka.fullname" . }}-config
        {{- if .Values.persistence.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "soc4kafka.fullname" . }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
